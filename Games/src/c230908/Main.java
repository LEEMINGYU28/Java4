package c230908;

public class Main {

	public static void main(String[]args) {
		//Lambda
		// 람다식 : 메서드 그자체
		// 이름이 없다
		// 타입은 x
		// () > {} 이렇게적는걸 람다식이라고한다.
		// 매개변수는 있다.
		// 코드
		// Functional Programming
		// - 함수형 프로그래밍
		// main(() -> {})
		// for(int i : arr){} << forEach
		// foreach((int i)->{i * 2} << ?
		// -람다식을 매개변수로 받는 메서드를 만들 수 있다.
		// - interface 를 사용해서 미리 내용을 선언해준다.
	//Stream
	//	- 하나의 데이터 > 변수, 메서드 , 클래스 , 배열 ,
	//	String , 인터페이스 , Set , Map,
	FuncTest.test();
	
	StreamTest.test();
	
	}
}
